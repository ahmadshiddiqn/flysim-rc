diff --git a/src/JSBSim.cpp b/src/JSBSim.cpp
index bc563fee..3dee1916 100644
--- a/src/JSBSim.cpp
+++ b/src/JSBSim.cpp
@@ -330,7 +330,7 @@ int main(int argc, char* argv[])
   _clearfp();
   _controlfp(_controlfp(0, 0) & ~(_EM_INVALID | _EM_ZERODIVIDE | _EM_OVERFLOW),
            _MCW_EM);
-#elif defined(__GNUC__) && !defined(sgi) && !defined(__APPLE__)
+#elif defined(__GNUC__) && !defined(sgi) && !defined(__APPLE__) && !defined(__EMSCRIPTEN__)
   feenableexcept(FE_DIVBYZERO | FE_INVALID | FE_OVERFLOW);
 #endif
 
diff --git a/src/input_output/CMakeLists.txt b/src/input_output/CMakeLists.txt
index 1e19ca6a..925ab3f0 100644
--- a/src/input_output/CMakeLists.txt
+++ b/src/input_output/CMakeLists.txt
@@ -1,43 +1,53 @@
+# Socket/network I/O sources do not compile under Emscripten (no BSD sockets).
+# Conditionally exclude them so the WASM build links cleanly.
+if(EMSCRIPTEN)
+  set(SOCKET_SOURCES)
+  set(SOCKET_HEADERS)
+else()
+  set(SOCKET_SOURCES FGfdmSocket.cpp
+                     FGOutputSocket.cpp
+                     FGOutputFG.cpp
+                     FGInputSocket.cpp
+                     FGUDPInputSocket.cpp)
+  set(SOCKET_HEADERS FGfdmSocket.h
+                     FGOutputSocket.h
+                     FGOutputFG.h
+                     net_fdm.hxx
+                     FGInputSocket.h
+                     FGUDPInputSocket.h)
+endif()
+
 set(SOURCES FGGroundCallback.cpp
             FGPropertyManager.cpp
             FGScript.cpp
             FGXMLElement.cpp
             FGXMLParse.cpp
-            FGfdmSocket.cpp
             FGXMLFileRead.cpp
             FGOutputType.cpp
-            FGOutputFG.cpp
-            FGOutputSocket.cpp
             FGOutputFile.cpp
             FGOutputTextFile.cpp
             FGPropertyReader.cpp
             FGModelLoader.cpp
             FGInputType.cpp
-            FGInputSocket.cpp
-            FGUDPInputSocket.cpp
             string_utilities.cpp
-            FGLog.cpp)
+            FGLog.cpp
+            ${SOCKET_SOURCES})
 
 set(HEADERS FGGroundCallback.h
             FGPropertyManager.h
             FGScript.h
             FGXMLElement.h
             FGXMLParse.h
-            FGfdmSocket.h
             FGXMLFileRead.h
-            net_fdm.hxx
             string_utilities.h
             FGOutputType.h
-            FGOutputFG.h
-            FGOutputSocket.h
             FGOutputFile.h
             FGOutputTextFile.h
             FGPropertyReader.h
             FGModelLoader.h
             FGInputType.h
-            FGInputSocket.h
-            FGUDPInputSocket.h
-            FGLog.h)
+            FGLog.h
+            ${SOCKET_HEADERS})
 
 add_library(InputOutput OBJECT ${SOURCES})
 # For MinGW, we need to force _WIN32_WINNT to a quite recent value for FGfdmSocket
diff --git a/src/models/FGInput.cpp b/src/models/FGInput.cpp
index 9688afc6..8daf9be9 100644
--- a/src/models/FGInput.cpp
+++ b/src/models/FGInput.cpp
@@ -40,7 +40,9 @@ INCLUDES
 
 #include "FGInput.h"
 #include "FGFDMExec.h"
+#ifndef __EMSCRIPTEN__
 #include "input_output/FGUDPInputSocket.h"
+#endif
 #include "input_output/FGXMLFileRead.h"
 #include "input_output/FGModelLoader.h"
 #include "input_output/FGLog.h"
@@ -101,9 +103,21 @@ bool FGInput::Load(Element* el)
   type = to_upper(type);
 
   if (type.empty() || type == "SOCKET") {
+#ifndef __EMSCRIPTEN__
     Input = new FGInputSocket(FDMExec);
+#else
+    FGXMLLogging log(element, LogLevel::ERROR);
+    log << "Socket input not supported in WASM builds" << endl;
+    return false;
+#endif
   } else if (type == "QTJSBSIM") {
+#ifndef __EMSCRIPTEN__
     Input = new FGUDPInputSocket(FDMExec);
+#else
+    FGXMLLogging log(element, LogLevel::ERROR);
+    log << "UDP input not supported in WASM builds" << endl;
+    return false;
+#endif
   } else if (type != string("NONE")) {
     FGXMLLogging log(element, LogLevel::ERROR);
     log << "Unknown type of input specified in config file" << endl;
diff --git a/src/models/FGOutput.cpp b/src/models/FGOutput.cpp
index 9a8d47d6..b5469bc7 100644
--- a/src/models/FGOutput.cpp
+++ b/src/models/FGOutput.cpp
@@ -41,7 +41,9 @@ INCLUDES
 
 #include "FGOutput.h"
 #include "input_output/FGOutputTextFile.h"
+#ifndef __EMSCRIPTEN__
 #include "input_output/FGOutputFG.h"
+#endif
 #include "input_output/FGXMLFileRead.h"
 #include "input_output/FGModelLoader.h"
 #include "input_output/FGLog.h"
@@ -214,11 +216,23 @@ bool FGOutput::Load(int subSystems, std::string protocol, std::string type,
     OutputTextFile->SetDelimiter("\t");
     Output = OutputTextFile;
   } else if (type == "SOCKET") {
+#ifndef __EMSCRIPTEN__
     Output = new FGOutputSocket(FDMExec);
     name += ":" + port + "/" + protocol;
+#else
+    FGLogging log(LogLevel::ERROR);
+    log << "Socket output not supported in WASM builds\n";
+    return false;
+#endif
   } else if (type == "FLIGHTGEAR") {
+#ifndef __EMSCRIPTEN__
     Output = new FGOutputFG(FDMExec);
     name += ":" + port + "/" + protocol;
+#else
+    FGLogging log(LogLevel::ERROR);
+    log << "FlightGear output not supported in WASM builds\n";
+    return false;
+#endif
   } else if (type == "TERMINAL") {
     // Not done yet
   } else if (type != string("NONE")) {
@@ -267,9 +281,21 @@ bool FGOutput::Load(Element* document, const SGPath& dir)
   } else if (type == "TABULAR") {
     Output = new FGOutputTextFile(FDMExec);
   } else if (type == "SOCKET") {
+#ifndef __EMSCRIPTEN__
     Output = new FGOutputSocket(FDMExec);
+#else
+    FGLogging log(LogLevel::ERROR);
+    log << "Socket output not supported in WASM builds\n";
+    return false;
+#endif
   } else if (type == "FLIGHTGEAR") {
+#ifndef __EMSCRIPTEN__
     Output = new FGOutputFG(FDMExec);
+#else
+    FGLogging log(LogLevel::ERROR);
+    log << "FlightGear output not supported in WASM builds\n";
+    return false;
+#endif
   } else if (type == "TERMINAL") {
     // Not done yet
   } else if (type != string("NONE")) {
