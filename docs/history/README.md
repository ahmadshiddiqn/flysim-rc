# Historical Documents

The files in this directory are **archived work logs** from earlier development
phases. They describe problems that have since been fixed (cwrap wrappers,
Emscripten FS bugs, Python version pinning) and implementation states that no
longer match the codebase.

**Do not use these as current documentation.** For up-to-date docs, see the
parent `docs/` directory and the project README.
